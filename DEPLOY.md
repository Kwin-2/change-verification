# 内网部署指南（供 IT 同事使用）

## 一、这是什么

一个**纯静态网站**（HTML + CSS + JS + 加密数据文件），无需后端、无需数据库、无需构建编译。
内容：懋略设计部 PACK 零部件工具库（变更验证速查 / 技术要求速查 / 设计规则库）。

## 二、源码获取

Git 仓库（GitHub）：

```
https://github.com/Kwin-2/change-verification.git
```

分支：`main`，仓库根目录就是网站成品（入口 `index.html`），无需任何构建步骤。

> 仓库为私有。IT 拉取前请联系设计部（Kwin）获取访问令牌，或由设计部直接提供 zip 打包文件，效果相同。

## 三、部署要求（三选一，按公司现状）

| 方案 | 做法 | 说明 |
|---|---|---|
| A. 挂到现有内网系统 | 把仓库全部文件复制到现有系统的静态目录，入口指到 `index.html` | 改动最小，推荐 |
| B. Nginx / Apache | 站点根目录指向仓库文件目录即可 | 无需任何特殊配置 |
| C. IIS | 新建站点，物理路径指向文件目录，默认文档 `index.html` | 需确认 `.json` 的 MIME 类型为 `application/json`（IIS 默认已支持） |

**协议要求：HTTP 或 HTTPS 均可。**
页面内数据解密在 HTTPS 下走浏览器原生加密接口；在普通 HTTP 内网地址（如 `http://192.168.x.x`）下会自动切换到内置的纯 JS 解密模块（V1.2.2 起已内置，无需额外配置）。

**唯一硬性要求：必须通过 HTTP(S) 访问**，不能双击 `index.html` 用 `file://` 打开（浏览器安全限制会导致数据加载失败）。

## 四、目录结构

```
index.html            门户（唯一入口，含访问口令门）
spec.html             旧链接跳转页（保留兼容）
rules/                设计规则库（内嵌子应用）
  index.html
  assets/ (app.js / jscrypto.js / style.css)
  data/bundle.json    规则库数据（AES-GCM 加密存储）
```

## 五、访问控制

- 站点访问口令：**Morlus**（由设计部统一管理）。
- 已设置 `noindex,nofollow`，搜索引擎不会收录；仍请**仅部署在内网，不要映射到公网**。
- 规则库数据文件（bundle.json）为 AES-256-GCM 加密存储，即使文件被直接下载也无法读取内容。

## 六、部署后验收（2 分钟）

1. 浏览器打开站点地址 → 出现令牌输入页；
2. 输入 `Morlus` → 进入门户首页；
3. 左侧依次点「变更验证速查字典」「技术要求速查」「设计规则库」，三个工具均应正常显示内容；
4. 重点确认「设计规则库」**不再要求二次输入令牌**。

## 七、后续更新

设计部发布新版后，IT 只需在部署目录执行 `git pull`（或替换为新版文件）即可，无迁移脚本、无配置变更。

## 八、联系方式

部署中遇到任何问题（访问白屏、令牌不通过、样式异常），请截图联系设计部 Kwin。
